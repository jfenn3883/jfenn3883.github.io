function setup() {
  let canvasIndex = createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(settings.background_color);
}

function windowResized() {
  resizeCanvas(windowWidth, windowHeight);
}
